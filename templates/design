<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <title>Login - PayShield</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <!-- Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    body {
      margin: 0;
      font-family: 'Poppins', sans-serif;
      min-height: 100vh;
      display: flex;
      background-color: #f5f7fb;
    }

    .left-panel {
      background: linear-gradient(135deg, #004E89, #00AEEF);
      color: white;
      flex: 1;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      text-align: center;
      padding: 3rem;
      position: relative;
      overflow: hidden;
    }

    .left-panel::before {
      content: "";
      position: absolute;
      top: 0;
      left: -30%;
      width: 160%;
      height: 100%;
      background: url('https://www.transparenttextures.com/patterns/wavecut.png') repeat;
      opacity: 0.15;
      animation: moveWave 10s linear infinite;
    }

    @keyframes moveWave {
      0% { transform: translateX(0); }
      100% { transform: translateX(-100px); }
    }

    .left-panel h1 {
      font-size: 2.8rem;
      font-weight: 700;
      letter-spacing: 1px;
      margin-top: 1rem;
    }

    .left-panel p {
      font-size: 1.1rem;
      opacity: 0.9;
      margin-top: 0.5rem;
    }

    .logo {
      width: 100px;
      filter: drop-shadow(0 0 8px rgba(255, 255, 255, 0.5));
    }

    /* Right panel */
    .right-panel {
      flex: 1;
      display: flex;
      justify-content: center;
      align-items: center;
      background: linear-gradient(to bottom right, #e3f2fd, #f9fbfd);
      padding: 2rem;
    }

    .login-card {
      background: rgba(255, 255, 255, 0.9);
      border-radius: 20px;
      box-shadow: 0 8px 25px rgba(0, 0, 0, 0.1);
      padding: 2rem;
      width: 100%;
      max-width: 400px;
      transition: transform 0.3s ease;
    }

    .login-card:hover {
      transform: translateY(-5px);
    }

    .login-card h3 {
      color: #004E89;
      font-weight: 700;
      margin-bottom: 1rem;
    }

    .form-label {
      color: #333;
      font-weight: 500;
    }

    .form-control {
      border-radius: 10px;
      padding: 10px;
      border: 1px solid #ccc;
      transition: box-shadow 0.3s ease;
    }

    .form-control:focus {
      box-shadow: 0 0 10px rgba(0, 174, 239, 0.4);
      border-color: #00AEEF;
    }

    .btn-primary {
      background: linear-gradient(90deg, #004E89, #00AEEF);
      border: none;
      border-radius: 10px;
      padding: 0.75rem;
      font-weight: 600;
      color: #fff;
      transition: 0.3s;
    }

    .btn-primary:hover {
      background: linear-gradient(90deg, #00AEEF, #004E89);
      transform: scale(1.03);
    }

    a {
      color: #00AEEF;
      text-decoration: none;
      font-weight: 500;
    }

    a:hover {
      text-decoration: underline;
    }

    .footer-text {
      font-size: 0.85rem;
      color: #888;
      margin-top: 1rem;
    }

    @media (max-width: 768px) {
      body {
        flex-direction: column;
      }

      .left-panel {
        display: none;
      }

      .right-panel {
        flex: none;
        min-height: 100vh;
        background: linear-gradient(135deg, #004E89, #00AEEF);
      }

      .login-card {
        background: rgba(255, 255, 255, 0.95);
      }
    }
  </style>
</head>

<body>
  <div class="left-panel">
    <img src="https://raw.githubusercontent.com/sakhiya-yagnik/payshield/main/static/logo.png" alt="PayShield Logo" class="logo">
    <h1>PayShield</h1>
    <p>Secure Payments, Simplified</p>
  </div>

  <div class="right-panel">
    <div class="login-card">
      <h3 class="text-center">Login to Continue</h3>
      <p class="text-muted text-center mb-4">Access your PayShield dashboard</p>

      {% with messages = get_flashed_messages(with_categories=true) %}
      {% if messages %}
      {% for category, message in messages %}
      <div class="alert alert-{{ category }} alert-dismissible fade show">
        {{ message }}
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
      </div>
      {% endfor %}
      {% endif %}
      {% endwith %}

      <form method="POST">
        <div class="mb-3">
          <label for="email" class="form-label">Email Address</label>
          <input type="email" id="email" name="email" class="form-control" required>
        </div>

        <div class="mb-3">
          <label for="password" class="form-label">Password</label>
          <input type="password" id="password" name="password" class="form-control" required>
        </div>

        <button type="submit" class="btn btn-primary w-100">Login & Get OTP</button>
      </form>

      <div class="mt-3 text-center">
        <a href="/register">New user? Register</a>
      </div>
      <p class="footer-text text-center">© 2025 PayShield Secure Payment Gateway</p>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
===========================

<!-- {% if account.wallet_created %}
        <div class="mt-3 alert alert-success">Wallet is active ✅ (created on {{ account.wallet_created_at }})</div>
        {% else %}
        <div class="mt-3">
          <a href="{{ url_for('wallet') }}" class="btn btn-outline-primary w-100">Create Wallet</a>
        </div>
        {% endif %} -->
==========
qr share_qr
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Share UPI QR</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
</head>

<body class="text-center p-4 bg-light">

    <h4 class="mb-3">Your UPI QR Code</h4>
    <img id="qrImage" src="data:image/png;base64,{{ qr_base64 }}" alt="UPI QR" class="img-fluid rounded shadow"
        width="250">

    <p class="mt-3">
        <b>UPI ID:</b> {{ upi_id }}<br>
        <b>UPI Link:</b> <span class="text-primary">{{ upi_link }}</span>
    </p>

    <button class="btn btn-success mt-3" id="shareBtn">Share QR + Link</button>
    <!-- <button class="btn btn-success mt-3" id="downloadBtn">Download QR</button> -->
    <a href="{{ url_for('profile') }}" class="btn btn-secondary btn-sm mt-3">
        ← Back to Profile
    </a>

    <script>
        document.getElementById("shareBtn").addEventListener("click", async () => {
            const qrBase64 = document.getElementById("qrImage").src.split(",")[1];
            const blob = await (await fetch("data:image/png;base64," + qrBase64)).blob();
            const file = new File([blob], "upi_qr.png", { type: "image/png" });

            const upiLink = "{{ upi_link }}";
            const shareText = `Here's my UPI ID: {{ upi_id }}\nPay using this link: ${upiLink}`;

            if (navigator.canShare && navigator.canShare({ files: [file] })) {
                try {
                    await navigator.share({
                        title: "PayShield UPI QR",
                        text: shareText,
                        files: [file]
                    });
                } catch (err) {
                    console.error("Sharing canceled or failed:", err);
                    alert("Sharing canceled or not supported on this device.");
                }
            } else {
                alert("Your browser does not support image sharing. Please download the QR manually.");
            }
        });

        // Download fallback
        document.getElementById("downloadBtn").addEventListener("click", () => {
            const a = document.createElement("a");
            a.href = document.getElementById("qrImage").src;
            a.download = "upi_qr.png";
            a.click();
        });
    </script>
</body>

</html>


===
qr update
<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Share UPI QR - PayShield</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background: linear-gradient(120deg, #004E89, #00A6FB);
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      font-family: 'Poppins', sans-serif;
      margin: 0;
      padding: 20px;
      color: #333;
    }

    .qr-card {
      background: #fff;
      border-radius: 18px;
      box-shadow: 0 8px 25px rgba(0, 0, 0, 0.1);
      padding: 30px 25px;
      max-width: 420px;
      width: 100%;
      text-align: center;
      transition: all 0.3s ease-in-out;
    }

    .qr-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 12px 35px rgba(0, 0, 0, 0.15);
    }

    .header-logo {
      width: 55px;
      height: 55px;
      background: linear-gradient(135deg, #0077B6, #00B4D8);
      border-radius: 50%;
      color: #fff;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 26px;
      font-weight: bold;
      margin: 0 auto 10px;
    }

    .brand {
      color: #004E89;
      font-size: 1.5rem;
      font-weight: 700;
    }

    .qr-img {
      width: 250px;
      border-radius: 10px;
      box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
      margin-top: 10px;
    }

    .upi-info {
      background: #f8f9fa;
      border-radius: 10px;
      padding: 10px;
      font-size: 0.95rem;
      margin-top: 20px;
    }

    .upi-info b {
      color: #004E89;
    }

    .btn-success {
      background: #28a745;
      border: none;
      font-weight: 600;
      border-radius: 8px;
      padding: 10px;
      width: 100%;
    }

    .btn-success:hover {
      background: #218838;
    }

    .btn-secondary {
      background: #004E89;
      border: none;
      border-radius: 8px;
      padding: 8px 16px;
      font-size: 0.9rem;
    }

    .btn-secondary:hover {
      background: #003863;
    }

    .footer {
      margin-top: 20px;
      font-size: 0.85rem;
      color: #777;
    }

    /* Modal Styling */
    .modal-content {
      border-radius: 15px;
      padding: 20px;
      text-align: center;
    }

    .modal-header {
      border-bottom: none;
      justify-content: center;
    }

    .modal-title {
      font-weight: 600;
      color: #004E89;
    }

    .copy-btn {
      background: #00A6FB;
      color: white;
      border: none;
      border-radius: 8px;
      padding: 8px 16px;
      font-weight: 500;
      transition: all 0.3s;
    }

    .copy-btn:hover {
      background: #0077B6;
    }

    .modal-footer {
      border-top: none;
      justify-content: center;
    }
  </style>
</head>

<body>

  <div class="qr-card">
    <div class="header-logo">₹</div>
    <div class="brand">PayShield</div>
    <h5 class="text-muted mt-2 mb-4">Your UPI QR Code</h5>

    <img id="qrImage" src="data:image/png;base64,{{ qr_base64 }}" alt="UPI QR" class="qr-img">

    <div class="upi-info">
      <p><b>UPI ID:</b> {{ upi_id }}</p>
      <p><b>UPI Link:</b> <span class="text-primary">{{ upi_link }}</span></p>
    </div>

    <button class="btn btn-success mt-3" data-bs-toggle="modal" data-bs-target="#shareModal">📤 Share QR + Link</button>

    <a href="{{ url_for('profile') }}" class="btn btn-secondary w-100 mt-3">← Back to Profile</a>

    <div class="footer">© 2025 PayShield Secure Payments</div>
  </div>

  <!-- Share Modal -->
  <div class="modal fade" id="shareModal" tabindex="-1" aria-labelledby="shareModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
      <div class="modal-content shadow-lg">
        <div class="modal-header">
          <h5 class="modal-title" id="shareModalLabel">Share Your UPI QR</h5>
        </div>
        <div class="modal-body">
          <img src="data:image/png;base64,{{ qr_base64 }}" alt="UPI QR" class="img-fluid mb-3" width="200">
          <p><b>UPI ID:</b> {{ upi_id }}</p>
          <p><b>Link:</b> <span class="text-primary">{{ upi_link }}</span></p>
          <div class="d-grid gap-2 mt-3">
            <button class="copy-btn" onclick="copyText('{{ upi_id }}')">📋 Copy UPI ID</button>
            <button class="copy-btn" onclick="copyText('{{ upi_link }}')">🔗 Copy UPI Link</button>
            <button class="btn btn-success" id="shareNowBtn">📤 Share Now</button>
          </div>
        </div>
        <div class="modal-footer">
          <button class="btn btn-outline-secondary" data-bs-dismiss="modal">Close</button>
        </div>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <script>
    function copyText(text) {
      navigator.clipboard.writeText(text);
      alert("Copied: " + text);
    }

    document.getElementById("shareNowBtn").addEventListener("click", async () => {
      const qrBase64 = document.getElementById("qrImage").src.split(",")[1];
      const blob = await (await fetch("data:image/png;base64," + qrBase64)).blob();
      const file = new File([blob], "upi_qr.png", { type: "image/png" });
      const upiLink = "{{ upi_link }}";
      const shareText = `Here's my UPI ID: {{ upi_id }}\nPay using this link: ${upiLink}`;

      if (navigator.canShare && navigator.canShare({ files: [file] })) {
        await navigator.share({
          title: "PayShield UPI QR",
          text: shareText,
          files: [file]
        });
      } else {
        alert("Your browser doesn't support native sharing.");
      }
    });
  </script>

</body>

</html>